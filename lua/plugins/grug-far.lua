-- grug-far.nvim já está incluído no core do LazyVim (editor.lua)
-- com o keymap <leader>sr. Este ficheiro sobrepõe as opções se necessário.
return {
  {
    "MagicDuck/grug-far.nvim",
    opts = {
      headerMaxWidth = 80,
    },
  },
}
